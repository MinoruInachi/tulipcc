# This partition table is for the 16MiB Tab5 flash with OTA support.
#
# 'system' and 'vfs' mirror the Tulip CC layout (see
# esp32s3/boards/N16R8/tulip-partitions-16MB.csv): fs_create.py builds a
# littlefs image for each -- /sys holds the examples and images from
# tulip/fs/tulip, /user holds user files. Without them MicroPython registers a
# 'vfs' spanning the rest of flash at runtime (ports/esp32/main.c), which never
# appears in the partition table binary and so is invisible to fs_create.py.
#
# The app slots are 0x480000 (4.5 MiB). They started at the ESP32-S3's
# 0x390000, grew to 0x3f0000, and the port kept growing into that too: with
# the camera stack in, the TAB5 build stood at 0x3de570 with 72 KB to spare,
# and every feature since has been paid for by squeezing something else (-Os on
# the LVGL binding and ulab, mbedTLS curves off). 4.5 MiB puts about 650 KB
# back. It comes out of 'vfs', which drops from 5.06 MiB to 3.94 MiB -- a
# camera capture is about 250 KB, so that is still a dozen or more of them.
# 'system' stays 3 MiB: the /sys image is just under 2.9 MB of source.
#
# Moving 'system' and 'vfs' means a device flashed with the old table has to be
# written whole (partition table, app, sys and vfs images) and its /user
# restored by hand -- the old littlefs is at the wrong offset for the new
# table, and MicroPython formats what it finds there. Back /user up first.
#
# No 'drums' partition: TAB5 does not build amy_connector.c, so nothing mmaps
# the gamma9001 banks. Adding one later means taking 0x3a0000 out of 'vfs'.
#
# Sizes sum to exactly 0x1000000 (16MiB).
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x4000,
otadata,  data, ota,     0xd000,   0x2000,
phy_init, data, phy,     0xf000,   0x1000,
ota_0,    app,  ota_0,   0x10000,  0x480000,
ota_1,    app,  ota_1,   0x490000, 0x480000,
system,   data, fat,     0x910000, 0x300000,
vfs,      data, fat,     0xc10000, 0x3f0000,
